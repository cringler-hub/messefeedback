# Diese PHP-Dateien werden nur per require() eingebunden,
# nie direkt aufgerufen.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
